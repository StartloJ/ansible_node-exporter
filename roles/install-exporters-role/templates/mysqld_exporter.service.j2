[Unit]
Description=mysqld_exporter
Wants=network-online.target
After=network-online.target

[Service]
User={{user}}
ExecStart={{ binary_path }}/mysqld_exporter {{ mysqld_exporter_options }}
Restart=on-failure

[Install]
WantedBy=multi-user.target